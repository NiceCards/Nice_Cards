# Backend API origin, e.g. https://your-backend.onrender.com
# Set this on Netlify as an env var (VITE_API_URL) at build time.
# Leave empty for local development -> requests use the Vite dev proxy (/api, /uploads).
VITE_API_URL=
