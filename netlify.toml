[build]
  command = "npm run build"
  publish = "client/dist"

# SPA fallback: serve index.html for all client-side routes (except API/uploads,
# which are handled by the separate backend host via VITE_API_URL).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
